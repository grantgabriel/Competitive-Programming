#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL); cout.tie(NULL);

    int N, M;
    cin >> N >> M;

    int array[N], jarak;
    for(int i = 0; i < N; i++) {
        cin >> array[N];
    }

    for(int i = 0; i < N; i++)
        for(int j = 0; j < N; j++) {
            if(j == i) continue;
            
            if(array[i] < 0 && array[j] < 0)
        }

    return 0;
}