// General solution : 

#include <bits/stdc++.h>
#define ll long long
#define boost ios_base::sync_with_stdio(false); cin.tie(NULL);
using namespace std;

int main() {
    boost;
    
    ll N, K, M;
    cin >> N >> K >> M;

    if(M % 2 == 0) cout << "Kedua";
    else {}
    
    
    return 0;
}