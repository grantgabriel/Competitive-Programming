#include <bits/stdc++.h>
#define boost ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
using namespace std;

int main() {
    boost;


	int N, M, SUM;
    cin >> N >> M;
	vector<int> nums(N);
	for(auto &num : nums) {
		cin >> num;
	}

	for(int num : nums) {


    return 0;
}

